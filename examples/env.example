# Laneful Ruby SDK Examples Environment Variables
# Copy this file to .env and fill in your actual values

# Required: Your Laneful API endpoint
LANEFUL_BASE_URL=https://your-subdomain.z1.send.dev.laneful.net

# Required: Your Laneful API authentication token
LANEFUL_AUTH_TOKEN=priv-YourAuthTokenHere

# Required: Email address to send from (must be verified in your Laneful account)
LANEFUL_FROM_EMAIL=sender@yourdomain.com

# Required: Comma-separated list of recipient email addresses
LANEFUL_TO_EMAILS=recipient1@example.com,recipient2@example.com,recipient3@example.com

# Optional: Template ID for template email examples
LANEFUL_TEMPLATE_ID=your-template-id

# Optional: Webhook secret for webhook examples
LANEFUL_WEBHOOK_SECRET=your-webhook-secret-here

# Optional: Webhook URL for webhook examples
LANEFUL_WEBHOOK_URL=https://your-domain.com/webhook

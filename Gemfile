# frozen_string_literal: true

source 'https://rubygems.org'

ruby '>= 3.0.0'

# Specify your gem's dependencies in laneful-ruby.gemspec
gemspec

# Development dependencies
group :development, :test do
  gem 'bundler', '~> 2.0'
  gem 'rake', '~> 13.0'
  gem 'rspec', '~> 3.0'
  gem 'rubocop', '~> 1.0'
  gem 'webmock', '~> 3.0'
end
